const mdLinks = require('../');


describe('funciones', () => {

  it("dada una ruta relativa debe retornar false", () => {
    const flag = functions.tipoRutafuncion(pathRelative);
    expect(flag).toBe(false);
  });










  
  it('should...', () => {
    console.log('FIX ME!');
  });

});
